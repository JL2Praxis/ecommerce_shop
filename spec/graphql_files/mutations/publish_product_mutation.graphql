mutation PublishProduct($id: ID!) {
  publishProduct(input:{ id: $id }) {
    product {
      id
      name
      status
      createdAt
      updatedAt
    }
    error {
      message
      code
      type
    }
  }
}
